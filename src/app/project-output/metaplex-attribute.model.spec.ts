import { MetaplexAttribute } from './metaplex-attribute.model';

describe('MetaplexAttribute', () => {
  it('should create an instance', () => {
    expect(new MetaplexAttribute()).toBeTruthy();
  });
});
